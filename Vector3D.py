class Vector3D:
    def __init__(self, x=0.0, y=0.0, z=0.0):
        """
        Initialize a 3D vector with x, y, and z components.
        """
        self.x = float(x)
        self.y = float(y)
        self.z = float(z)